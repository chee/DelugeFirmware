<template webc:root title="SCROLL▼▲">
	<svg viewBox="0 0 100 100">
		<circle cx="50" cy="50" r="40"/>
		<text x="22" y="60">▼▲</text>
	</svg>
</template>

<style>
	scroll-v-encoder svg {
		height: 2em;
	}

	scroll-v-encoder circle {
		stroke: var(--scroll-encoder-line);
		stroke-width: 4;
		fill: var(--scroll-encoder-fill);
	}

	scroll-v-encoder text {
		stroke: var(--scroll-encoder-line);
		fill: var(--scroll-encoder-line);
		font-size: 1.4em;
	}
</style>
