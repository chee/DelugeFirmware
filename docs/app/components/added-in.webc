<span class="added-in">
	added in <b><slot></slot></b>
</span>


<style>
	.added-in {
		background: color-mix(in srgb, var(--line) 95%, var(--fill));
		color: var(--fill);
		border: 1px solid color-mix(in srgb, var(--line) 20%, var(--fill));
		padding: 0 .24em;
		letter-spacing: -1px;
		border-radius: 4px;
	}
</style>
